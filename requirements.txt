# FastAPI
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.18
httpx==0.28.1
sse-starlette==2.2.1

# Production Server
gunicorn==23.0.0

# Rate Limiting
slowapi==0.1.9

# Redis for caching, rate limiting, request deduplication
redis==5.2.1
aioredis==2.0.1

# MySQL for AI tracking (session management, token usage)
aiomysql==0.2.0

# LLM SDKs
anthropic>=0.40.0
openai>=1.76.0
# Gemini — added in Phase 8 of the CFA rewrite. Becomes the default after
# the bench (scripts/bench_providers.py) confirms tool-use + Kirun-compile
# parity with Claude Haiku. NOTE: Google deprecated `google-generativeai`
# in favor of `google-genai` — this still works (last release Aug 2025)
# but should be swapped before Python 3.10+ migration. See
# https://github.com/google-gemini/deprecated-generative-ai-python
google-generativeai>=0.8.0

# Kirun DSL compiler / decompiler — used by the modlix-mcp tool port for
# function authoring. Provides DSLCompiler.compile/decompile/validate.
kirun-py>=0.1

# Eureka Client
py-eureka-client==0.11.10

# Image Processing (compression before sending to LLM APIs)
Pillow>=10.3.0,<11.0.0

# Web Scraping (Adzump business analysis)
beautifulsoup4==4.13.5
playwright==1.55.0

# Utilities
python-dotenv==1.0.1
pydantic==2.10.4
pydantic-settings==2.7.1
