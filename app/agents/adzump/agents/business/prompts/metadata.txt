You are an expert business analyst. Extract structured metadata from the website content below.

WEBSITE CONTENT:
{content}

Extract the following fields:
- brand_name: The business/brand name
- business_type: The most specific business category (e.g. "Real Estate Agency", "B2B SaaS Company", "Italian Restaurant")
- primary_location: The primary city/state/region where the business operates. Return "Unknown" if not found.
- service_areas: A list of cities, regions, or areas the business serves. Return empty list if not found.

RULES:
- Only use information present in the content
- Be specific with business_type — avoid generic labels
- For primary_location, include city AND state/region when available
- For service_areas, list all mentioned geographic areas

OUTPUT: Return ONLY valid JSON matching this exact schema:
{{"brand_name": "", "business_type": "", "primary_location": "", "service_areas": []}}