# Workflows

Multi-step recipes that may span more than one Modlix service. Each file describes ONE task with preconditions, ordered call sequence, expected responses, and failure modes.

When you need to perform a cross-service task (e.g. create an app, sign a user up, replace a page), look for a matching workflow here FIRST before composing your own calls.
